lab4 目标代码生成
实现：
基本四则运算
跳转指令
函数调用
return
数组仅实现了定义，但是访问仍存在部分问题未解决，不能正常测试

测试用例：

int a,b,c;
float m,n;
int fibo(int a)
{
    if (a == 1 || a == 2) return 1;
    return fibo(a-1)+fibo(a-2);
}

int main()
{
    int m,n,i;
    m = read();
    i=1;
    while(i<=m)
    {
        n = fibo(i);
        write(n);
        i=i+1;
    }
    return 1;
}