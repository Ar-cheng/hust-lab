进入主界面后两个选项：
1.SAT求解器
可以重复加载文件，不需要多次打开.exe文件，当前打开的文件会在界面最上方显示出来，
如果需要打开新的文件，直接进入1命令加载文件即可。求解CNF为命令2，命令3为优化
后的DPLL及优化效果。（ps：在一次加载文件后只可进行一次求解过程，如需要重新求解
需要再次加载该文件）
2.数独游戏
命令一生成数独，可重复执行生成不同的数独（并且该数独具有唯一解）
命令二求解最近一次生成的数独